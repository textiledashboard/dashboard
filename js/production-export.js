(function(){
function png(panel){const svg=panel.querySelector('.svg-chart svg'),d=window.productionExportData;if(!svg||!d)return;const clone=svg.cloneNode(true);clone.setAttribute('xmlns','http://www.w3.org/2000/svg');const xml=new XMLSerializer().serializeToString(clone),blob=new Blob([xml],{type:'image/svg+xml'}),url=URL.createObjectURL(blob),img=new Image();img.onload=()=>{const canvas=document.createElement('canvas');canvas.width=1800;canvas.height=620;const ctx=canvas.getContext('2d');ctx.fillStyle='#ffffff';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.drawImage(img,0,0,canvas.width,canvas.height);const a=document.createElement('a');a.href=canvas.toDataURL('image/png');a.download=`production-${d.key.toLowerCase().replace(/[^a-z0-9]+/g,'-')}-${panel.dataset.chart}.png`;a.click();URL.revokeObjectURL(url);if(window.notify)window.notify('Selected production chart PNG downloaded');};img.src=url;}
function save(panel){const d=window.productionExportData;if(!d)return;const type=panel.dataset.chart;let rows=[];if(type==='trend')rows=[['Year',`${d.key} production (${d.unit})`],...d.years.map((y,i)=>[y,d.trend[i]])];if(type==='variety')rows=[['Year',...d.series.map(s=>s[0])],...d.years.map((y,i)=>[y,...d.series.map(s=>s[1][i])])];if(type==='state')rows=[['State','Previous year','Latest year'],...d.states];const csv=rows.map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=`production-${d.key.toLowerCase().replace(/[^a-z0-9]+/g,'-')}-${type}.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500);if(window.notify)window.notify('Selected production chart CSV downloaded');}
document.querySelectorAll('#productionDashboard .production-chart').forEach(panel=>{const buttons=panel.querySelectorAll('.chart-toolbar button');if(buttons[0])buttons[0].onclick=()=>png(panel);if(buttons[1])buttons[1].onclick=()=>save(panel);});
})();
